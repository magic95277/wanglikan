# “往里看”项目入门说明

这份说明不要求你懂计算机语言。先把整个网站理解成三个人配合工作：

1. **手机页面**负责接收问题、显示等待画面和结果。
2. **网站后端**负责检查问题，并安全地联系 DeepSeek。
3. **DeepSeek**负责按照我们规定的格式生成分析结果。

## 一次分析是怎样完成的

1. 用户打开网站，CloudBase 把网页发送到手机。
2. 用户输入“结婚应该给彩礼吗”，点击“开始分析”。
3. `public/app.js` 把问题发送到网站后端的 `/api/analyze`。
4. `server.js` 检查问题是否为空、是否过长。
5. `server.js` 把问题放进提示词，使用服务端密钥调用 DeepSeek。
6. DeepSeek 返回标题、六层分析、结论和一句总结。
7. `public/app.js` 把六层分析做成堆叠卡片，显示在手机上。

可以把它简单记成：

```text
用户输入 → 手机页面 → 网站后端 → DeepSeek
用户看到结果 ← 堆叠卡片 ← 网站后端 ← DeepSeek 回复
```

## 每个文件是做什么的

| 文件 | 可以把它理解成 | 主要用途 |
| --- | --- | --- |
| `public/index.html` | 房子的结构 | 页面上有哪些标题、按钮和输入框 |
| `public/styles.css` | 装修和外观 | 颜色、大小、间距、堆叠卡片样式 |
| `public/app.js` | 页面上的动作 | 点击按钮、切换画面、发送问题、显示结果 |
| `server.js` | 网站后台工作人员 | 检查输入、调用 DeepSeek、返回结果 |
| `Dockerfile` | 给 CloudBase 的装箱说明 | 告诉云端怎样启动网站 |
| `cloudbaserc.json` | CloudBase 地址标签 | 记录环境和服务名称 |
| `package.json` | 项目基本信息 | 记录启动和检查命令 |

## 你以后最可能修改的地方

### 修改页面文字

打开 `public/index.html`。例如首页标题、输入框提示语、示例问题都在这里。

### 修改颜色和卡片外观

打开 `public/styles.css`。文件最上方的 `--green`、`--orange` 等内容是全站颜色；`.layer-card` 是六层堆叠卡片。

### 修改分析层数或判断标准

打开 `server.js`，找到 `singlePrompt`。这段中文就是交给 DeepSeek 的要求。修改这里会直接影响 AI 返回什么内容。

### 修改观点对比方式

打开 `server.js`，找到 `comparePrompt`。这里规定了共同点、冲突点和最终关系。

### 修改结果怎样显示

打开 `public/app.js`，找到 `renderSingle` 或 `renderCompare`。

## 为什么密钥没有写在代码里

DeepSeek 密钥相当于一张付费门卡。如果把它写进公开的 GitHub，其他人可以复制并消耗你的额度。因此代码只写：

```js
process.env.DEEPSEEK_API_KEY
```

它的意思是“去 CloudBase 的安全配置中读取密钥”，而不是把真实密钥写在网页里。

## 网站现在放在哪里

- GitHub 保存源码，类似“代码仓库”。
- CloudBase 负责运行源码，类似“一台一直开着的云电脑”。
- DeepSeek API 负责生成分析内容。

修改 GitHub 代码不会自动改变正在运行的网站。修改完成后，还需要重新部署到 CloudBase。

## 常见词语翻译

- **前端**：用户在手机上直接看到和点击的部分。
- **后端**：用户看不到、在云端运行的处理部分。
- **API**：两个程序之间传递问题和答案的通道。
- **部署**：把本地或 GitHub 的代码更新到云端，让网站运行新版本。
- **环境变量**：云端保存配置和密钥的位置。
- **JSON**：程序之间传递信息的一种固定格式。
